console.log("loops");

const colors = ['silver', 'red', 'gold', 'aqua', 'violet'];

const colorLoop = () => {
    for(let i = 0; i < colors.length; i++) {
        console.log(colors[i]);
    }
};

colorLoop();